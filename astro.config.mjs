// @ts-check
import { defineConfig } from 'astro/config';

// Published on GitHub Pages at https://mlangsman.github.io/make-change-site/
// With a custom domain, set `site` to it and remove `base`.
export default defineConfig({
  site: 'https://mlangsman.github.io',
  base: '/make-change-site',
});
